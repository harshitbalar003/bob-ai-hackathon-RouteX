numpy>=1.26.0
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.7.1
pydantic-settings>=2.2.1
pydantic[email]>=2.7.1
sqlalchemy>=2.0.30
aiosqlite>=0.20.0
greenlet>=3.0.3
pyyaml>=6.0.1
httpx>=0.27.0
python-multipart>=0.0.9
sse-starlette>=2.1.0
alembic>=1.13.1
# Authentication
bcrypt>=4.1.0
PyJWT>=2.8.0
pytest>=8.0.0
pytest-asyncio>=0.23.0
anyio>=4.3.0
# ML layer — Model 1 (Excursion Forecaster). scikit-learn is the ONLY ML
# dependency permitted. No XGBoost, LightGBM, SHAP, or deep learning.
# Pin exactly: trained artifacts are unpickled and minor version changes warn/break.
scikit-learn==1.5.2
# pandas and pyarrow are corpus-generation + training deps; not required at serve time.
# Install with: pip install pandas pyarrow
# Optional watsonx.ai — only installed if WATSONX_ENABLED=true
# ibm-watsonx-ai>=1.0.9
